Adds a new fertilizing device to an existing device, if one of the specified type doesn’t already exist.
    ---
    tags:
        - Fertilizing Devices
    description: Adds a new fertilizing device for a device, specified by device_id, with a default type 'Pump'.
    parameters:
        - name: device_id
          in: body
          type: integer
          required: true
          description: ID of the device to add the fertilizing device to.
        - name: device_type
          in: body
          type: string
          required: false
          default: Pump
          description: Type of fertilizing device.
    responses:
        201:
            description: Fertilizing device successfully added.
        400:
            description: Fertilizing device of specified type already exists for this device.